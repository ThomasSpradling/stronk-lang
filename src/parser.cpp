#include "parser.h"

// Adds a sinle token to the token list.
void Parser::AddToken(const Token &token) {

}

// Parses the token list and returns corresponding bytecode.
auto Parser::Parse() -> Bytecode {
    return {};
}